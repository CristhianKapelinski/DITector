http {
    server {
        listen 80;
        server_name example.com;
        location /file {
            autoindex on;
            root /var/www/example.com;
            index index.html;
        }
    }
}
